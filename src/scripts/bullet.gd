extends Entity
class_name Bullet


'''func _ready():
	rotation = direction.angle() + PI/2
	super()'''


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	super(delta)
